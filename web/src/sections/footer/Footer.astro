---
// import LegalLinks from "@/landings/legal/sections/LegalLinks/LegalLinks.astro";
import type { LANGUAGE } from "@global-shared/translations/domain/languages";
import LanguageSelector from "@/ui/LanguageSelector/LanguageSelector.astro";

type Props = {
  language: LANGUAGE;
  id?: string;
};

const { language, id } = Astro.props;
---

<footer class="o-footer">
  <LanguageSelector id={id} language={language} />
  <!-- <LegalLinks language={language}  /> -->
</footer>
